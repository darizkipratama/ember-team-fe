<div class="bg-blue-500 text-white p-8 rounded-lg shadow-lg">
    {{!-- template-lint-disable no-curly-component-invocation --}}
    <LinkTo @route="index" 
        class="bg-transparent 
            hover:bg-blue-500 
            text-red-400 
            font-semibold 
            hover:text-grey-400 py-2 px-4 border border-blue-500 hover:border-transparent rounded">
        &lt; Back to Team List
    </LinkTo>
    <p class="text-lg">Details of Team : {{@model.teamName}}</p>

    <Memberlist @teamId={{@model.id}}/>
</div>